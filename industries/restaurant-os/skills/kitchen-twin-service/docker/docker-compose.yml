version: '3.8'

services:
  kitchen-twin-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: kitchen-twin-service
    restart: unless-stopped
    ports:
      - "4015:4015"
    environment:
      - NODE_ENV=production
      - SERVICE_NAME=kitchen-twin-service
      - SERVICE_PORT=4015
      - MONGODB_URI=mongodb://mongodb:27017/kitchen_twin_service
      - RABBITMQ_URI=amqp://rabbitmq:5672
      - LOG_LEVEL=info
    depends_on:
      - mongodb
      - rabbitmq
    networks:
      - restaurant-os-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4015/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  restaurant-os-network:
    external: true
