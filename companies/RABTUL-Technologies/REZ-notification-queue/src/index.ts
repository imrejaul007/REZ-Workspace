import express from'express';import logger from'./utils/logger';import notifRoutes from'./routes/notifications';const app=express();const PORT=4321;app.use(express.json());app.get('/health',(_,res)=>res.json({service:'REZ Notification Queue',status:'healthy'}));app.use('/api/notifications',notifRoutes);app.listen(PORT,()=>logger.info(`REZ Notification Queue running on ${PORT}`));export{app};
