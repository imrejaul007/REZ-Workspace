apiVersion: apps/v1
kind: Deployment
metadata:
  name: ad-bazaar-snapchat-integration
  namespace: adbazaar
  labels:
    app: ad-bazaar-snapchat-integration
    tier: social-automation
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ad-bazaar-snapchat-integration
  template:
    metadata:
      labels:
        app: ad-bazaar-snapchat-integration
        tier: social-automation
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5104"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: snapchat-integration
        image: adbazaar/snapchat-integration:latest
        ports:
        - containerPort: 5104
          name: http
        env:
        - name: PORT
          value: "5104"
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: ad-bazaar-snapchat-integration-config
              key: NODE_ENV
        - name: MONGODB_URI
          valueFrom:
            configMapKeyRef:
              name: ad-bazaar-snapchat-integration-config
              key: MONGODB_URI
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: ad-bazaar-snapchat-integration-config
              key: REDIS_URL
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: adbazaar-secrets
              key: jwt-secret
        - name: SNAPCHAT_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: adbazaar-secrets
              key: snapchat-client-id
        - name: SNAPCHAT_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: adbazaar-secrets
              key: snapchat-client-secret
        - name: SNAPCHAT_AD_ACCOUNT_ID
          valueFrom:
            secretKeyRef:
              name: adbazaar-secrets
              key: snapchat-ad-account-id
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 5104
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 5104
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: app-config
          mountPath: /app/config
          readOnly: true
      volumes:
      - name: app-config
        configMap:
          name: ad-bazaar-snapchat-integration-config