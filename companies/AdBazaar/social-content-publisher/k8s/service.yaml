apiVersion: v1
kind: Service
metadata:
  name: ad-bazaar-social-publisher
  labels:
    app: ad-bazaar-social-publisher
    tier: social-automation
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5083
    protocol: TCP
    name: http
  selector:
    app: ad-bazaar-social-publisher