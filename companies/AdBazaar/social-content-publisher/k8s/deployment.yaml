apiVersion: apps/v1
kind: Deployment
metadata:
  name: ad-bazaar-social-publisher
  labels:
    app: ad-bazaar-social-publisher
    tier: social-automation
    component: publisher
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ad-bazaar-social-publisher
  template:
    metadata:
      labels:
        app: ad-bazaar-social-publisher
        tier: social-automation
        component: publisher
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5083"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: app
        image: adbazaar/social-content-publisher:latest
        ports:
        - containerPort: 5083
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: ad-bazaar-social-publisher-config
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 5083
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 5083
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3