apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ad-bazaar-youtube-integration
  labels:
    app: ad-bazaar-youtube-integration
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rate-limit: "100"
spec:
  tls:
  - hosts:
    - youtube-integration.adbazaar.internal
    secretName: ad-bazaar-youtube-integration-tls
  rules:
  - host: youtube-integration.adbazaar.internal
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ad-bazaar-youtube-integration
            port:
              number: 80