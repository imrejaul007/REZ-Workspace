# ================================================
# REZ-Merchant Kubernetes Service Template
# ================================================

apiVersion: v1
kind: Service
metadata:
  name: rez-cross-merchant-service
  namespace: rez-merchant
  labels:
    app: rez-cross-merchant-service
    managed-by: rez-merchant
  annotations:
    description: "rez-cross-merchant-service service"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4093
      targetPort: http
      protocol: TCP
  selector:
    app: rez-cross-merchant-service

---

# Headless service for StatefulSets (if needed)
apiVersion: v1
kind: Service
metadata:
  name: rez-cross-merchant-service-internal
  namespace: rez-merchant
  labels:
    app: rez-cross-merchant-service
    managed-by: rez-merchant
  annotations:
    description: "rez-cross-merchant-service internal headless service"
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 4093
      targetPort: http
      protocol: TCP
  selector:
    app: rez-cross-merchant-service