# ================================================
# REZ-Merchant Kubernetes Service Template
# ================================================

apiVersion: v1
kind: Service
metadata:
  name: REZ-merchant-loans-service
  namespace: rez-merchant
  labels:
    app: REZ-merchant-loans-service
    managed-by: rez-merchant
  annotations:
    description: "REZ-merchant-loans-service service"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3081
      targetPort: http
      protocol: TCP
  selector:
    app: REZ-merchant-loans-service

---

# Headless service for StatefulSets (if needed)
apiVersion: v1
kind: Service
metadata:
  name: REZ-merchant-loans-service-internal
  namespace: rez-merchant
  labels:
    app: REZ-merchant-loans-service
    managed-by: rez-merchant
  annotations:
    description: "REZ-merchant-loans-service internal headless service"
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 3081
      targetPort: http
      protocol: TCP
  selector:
    app: REZ-merchant-loans-service