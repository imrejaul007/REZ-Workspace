# Kubernetes ServiceAccount for REZ-dashboard
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-dashboard-sa
  namespace: rez-merchant
  labels:
    app: rez-dashboard
    managed-by: rez-merchant
  annotations:
    description: Service account for REZ-dashboard

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-dashboard-pdb
  namespace: rez-merchant
  labels:
    app: rez-dashboard
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-dashboard

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-dashboard-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-dashboard
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 3000
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
