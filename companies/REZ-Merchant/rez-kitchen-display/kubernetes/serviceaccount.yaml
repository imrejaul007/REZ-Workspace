# Kubernetes ServiceAccount for rez-kitchen-display
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-kitchen-display-sa
  namespace: rez-merchant
  labels:
    app: rez-kitchen-display
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-kitchen-display

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-kitchen-display-pdb
  namespace: rez-merchant
  labels:
    app: rez-kitchen-display
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-kitchen-display

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-kitchen-display-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-kitchen-display
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4014
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
