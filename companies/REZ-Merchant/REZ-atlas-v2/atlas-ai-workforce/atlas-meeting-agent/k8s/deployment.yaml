apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-meeting-agent
  namespace: rez-merchant
  labels:
    app: atlas-meeting-agent
    version: latest
    managed-by: rez-merchant
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-meeting-agent
  template:
    metadata:
      labels:
        app: atlas-meeting-agent
        version: latest
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5176"
    spec:
      containers:
        - name: atlas-meeting-agent
          image: ghcr.io/imrejaul007/atlas-meeting-agent:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 5176
              protocol: TCP
          envFrom:
            - configMapRef:
                name: atlas-meeting-agent-config
            - secretRef:
                name: atlas-meeting-agent-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp-dir
              mountPath: /tmp
      volumes:
        - name: tmp-dir
          emptyDir: {}