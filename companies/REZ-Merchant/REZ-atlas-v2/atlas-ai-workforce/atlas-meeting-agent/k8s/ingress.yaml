apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atlas-meeting-agent
  namespace: rez-merchant
  labels:
    app: atlas-meeting-agent
    managed-by: rez-merchant
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - api.rezmerchant.com
      secretName: atlas-meeting-agent-tls
  rules:
    - host: api.rezmerchant.com
      http:
        paths:
          - path: /api/meetings
            pathType: Prefix
            backend:
              service:
                name: atlas-meeting-agent
                port:
                  number: 5176