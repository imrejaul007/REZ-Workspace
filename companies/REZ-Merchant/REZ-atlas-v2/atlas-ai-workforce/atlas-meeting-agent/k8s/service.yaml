apiVersion: v1
kind: Service
metadata:
  name: atlas-meeting-agent
  namespace: rez-merchant
  labels:
    app: atlas-meeting-agent
    managed-by: rez-merchant
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5176
      targetPort: http
      protocol: TCP
  selector:
    app: atlas-meeting-agent