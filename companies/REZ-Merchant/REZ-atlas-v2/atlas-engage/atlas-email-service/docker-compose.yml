version: '3.8'

services:
  atlas-email-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: atlas-email-service
    restart: unless-stopped
    ports:
      - "5161:5161"
    environment:
      NODE_ENV: production
      PORT: 5161
      SERVICE_NAME: atlas-email-service
      MONGODB_URI: mongodb://mongodb:27017/atlas-email-service
      LOG_LEVEL: info
    depends_on:
      mongodb:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5161/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - rez-network

  mongodb:
    image: mongo:6-alpine
    container_name: email-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: atlas-email-service
    ports:
      - "27020:27017"
    volumes:
      - email-mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - rez-network

networks:
  rez-network:
    driver: bridge

volumes:
  email-mongo-data:
    driver: local