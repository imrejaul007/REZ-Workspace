apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-pipeline
  namespace: rez-merchant
  labels:
    app: atlas-pipeline
    version: latest
    managed-by: rez-merchant
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-pipeline
  template:
    metadata:
      labels:
        app: atlas-pipeline
        version: latest
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5181"
    spec:
      containers:
        - name: atlas-pipeline
          image: ghcr.io/imrejaul007/atlas-pipeline:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 5181
              protocol: TCP
          envFrom:
            - configMapRef:
                name: atlas-pipeline-config
            - secretRef:
                name: atlas-pipeline-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp-dir
              mountPath: /tmp
      volumes:
        - name: tmp-dir
          emptyDir: {}