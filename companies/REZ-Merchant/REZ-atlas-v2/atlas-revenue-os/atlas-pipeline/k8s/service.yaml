apiVersion: v1
kind: Service
metadata:
  name: atlas-pipeline
  namespace: rez-merchant
  labels:
    app: atlas-pipeline
    managed-by: rez-merchant
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5181
      targetPort: http
      protocol: TCP
  selector:
    app: atlas-pipeline