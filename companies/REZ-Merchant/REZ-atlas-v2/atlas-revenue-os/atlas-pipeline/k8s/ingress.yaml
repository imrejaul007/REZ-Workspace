apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atlas-pipeline
  namespace: rez-merchant
  labels:
    app: atlas-pipeline
    managed-by: rez-merchant
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - api.rezmerchant.com
      secretName: atlas-pipeline-tls
  rules:
    - host: api.rezmerchant.com
      http:
        paths:
          - path: /api/pipeline
            pathType: Prefix
            backend:
              service:
                name: atlas-pipeline
                port:
                  number: 5181