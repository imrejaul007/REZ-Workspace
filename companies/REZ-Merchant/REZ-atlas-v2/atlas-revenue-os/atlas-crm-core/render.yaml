services:
  - type: web
    name: atlas-crm-core
    env: node
    region: oregon
    plan: standard
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5183
      - key: SERVICE_NAME
        value: atlas-crm-core
      - key: MONGODB_URI
        sync: false
      - key: LOG_LEVEL
        value: info