apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atlas-crm-core
  namespace: rez-merchant
  labels:
    app: atlas-crm-core
    managed-by: rez-merchant
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - api.rezmerchant.com
      secretName: atlas-crm-core-tls
  rules:
    - host: api.rezmerchant.com
      http:
        paths:
          - path: /api/crm
            pathType: Prefix
            backend:
              service:
                name: atlas-crm-core
                port:
                  number: 5183