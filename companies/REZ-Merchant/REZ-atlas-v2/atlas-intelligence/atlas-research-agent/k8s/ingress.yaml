apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atlas-research-agent
  namespace: rez-merchant
  labels:
    app: atlas-research-agent
    managed-by: rez-merchant
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - api.rezmerchant.com
      secretName: atlas-research-agent-tls
  rules:
    - host: api.rezmerchant.com
      http:
        paths:
          - path: /api/research
            pathType: Prefix
            backend:
              service:
                name: atlas-research-agent
                port:
                  number: 5158