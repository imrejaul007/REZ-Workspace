apiVersion: v1
kind: Service
metadata:
  name: atlas-research-agent
  namespace: rez-merchant
  labels:
    app: atlas-research-agent
    managed-by: rez-merchant
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5158
      targetPort: http
      protocol: TCP
  selector:
    app: atlas-research-agent