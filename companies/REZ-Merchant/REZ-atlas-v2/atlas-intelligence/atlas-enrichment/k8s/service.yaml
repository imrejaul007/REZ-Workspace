apiVersion: v1
kind: Service
metadata:
  name: atlas-enrichment
  namespace: rez-merchant
  labels:
    app: atlas-enrichment
    managed-by: rez-merchant
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5160
      targetPort: http
      protocol: TCP
  selector:
    app: atlas-enrichment