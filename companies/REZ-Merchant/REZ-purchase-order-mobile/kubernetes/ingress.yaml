# ================================================
# REZ-Merchant Kubernetes Ingress Template
# ================================================

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: REZ-purchase-order-mobile
  namespace: rez-merchant
  labels:
    app: REZ-purchase-order-mobile
    managed-by: rez-merchant
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Request-ID $request_id";
spec:
  tls:
    - hosts:
        - ${DOMAIN:-api.rezmerchant.com}
      secretName: REZ-purchase-order-mobile-tls
  rules:
    - host: ${DOMAIN:-api.rezmerchant.com}
      http:
        paths:
          - path: /${SERVICE_PATH:-}
            pathType: Prefix
            backend:
              service:
                name: REZ-purchase-order-mobile
                port:
                  number: 4059
            properties:
              weight: null
            port:
              number: 4059

---

# Alternative: Path-based routing for multiple services
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: REZ-purchase-order-mobile-prefix
  namespace: rez-merchant
  labels:
    app: REZ-purchase-order-mobile
    managed-by: rez-merchant
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  tls:
    - hosts:
        - ${DOMAIN:-api.rezmerchant.com}
      secretName: REZ-purchase-order-mobile-tls
  rules:
    - host: ${DOMAIN:-api.rezmerchant.com}
      http:
        paths:
          - path: /${SERVICE_PATH:-}/(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: REZ-purchase-order-mobile
                port:
                  number: 4059