# Kubernetes ServiceAccount for rez-payroll
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-payroll-sa
  namespace: rez-merchant
  labels:
    app: rez-payroll
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-payroll

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-payroll-pdb
  namespace: rez-merchant
  labels:
    app: rez-payroll
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-payroll

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-payroll-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-payroll
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4610
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
