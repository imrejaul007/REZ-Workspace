# Kubernetes ServiceAccount for REZ-atlas-routes
apiVersion: v1
kind: ServiceAccount
metadata:
  name: REZ-atlas-routes-sa
  namespace: rez-merchant
  labels:
    app: REZ-atlas-routes
    managed-by: rez-merchant
  annotations:
    description: Service account for REZ-atlas-routes

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: REZ-atlas-routes-pdb
  namespace: rez-merchant
  labels:
    app: REZ-atlas-routes
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: REZ-atlas-routes

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: REZ-atlas-routes-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: REZ-atlas-routes
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 5171
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
