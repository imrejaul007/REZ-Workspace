# Kubernetes ServiceAccount for REZ-multi-warehouse
apiVersion: v1
kind: ServiceAccount
metadata:
  name: REZ-multi-warehouse-sa
  namespace: rez-merchant
  labels:
    app: REZ-multi-warehouse
    managed-by: rez-merchant
  annotations:
    description: Service account for REZ-multi-warehouse

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: REZ-multi-warehouse-pdb
  namespace: rez-merchant
  labels:
    app: REZ-multi-warehouse
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: REZ-multi-warehouse

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: REZ-multi-warehouse-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: REZ-multi-warehouse
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4203
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
