# Render Blueprint for rez-supplier-marketplace
# Deploy at: https://dashboard.render.com/blueprints

services:
  - type: web
    name: rez-supplier-marketplace
    env: node
    region: singapore
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4630
      - key: MONGODB_URI
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        sync: false
