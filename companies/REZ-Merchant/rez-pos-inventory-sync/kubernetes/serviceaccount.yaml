# Kubernetes ServiceAccount for rez-pos-inventory-sync
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-pos-inventory-sync-sa
  namespace: rez-merchant
  labels:
    app: rez-pos-inventory-sync
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-pos-inventory-sync

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-pos-inventory-sync-pdb
  namespace: rez-merchant
  labels:
    app: rez-pos-inventory-sync
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-pos-inventory-sync

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-pos-inventory-sync-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-pos-inventory-sync
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4031
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
