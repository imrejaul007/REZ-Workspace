# Kubernetes ConfigMap for REZ-merchant-trust-bridge
apiVersion: v1
kind: ConfigMap
metadata:
  name: REZ-merchant-trust-bridge-config
  namespace: rez-merchant
  labels:
    app: REZ-merchant-trust-bridge
    managed-by: rez-merchant
data:
  NODE_ENV: "production"
  PORT: "4041"
  LOG_LEVEL: "info"
  SERVICE_NAME: "REZ-merchant-trust-bridge"
  SERVICE_VERSION: "latest"
  FEATURE_METRICS_ENABLED: "true"
  FEATURE_TRACING_ENABLED: "true"
  CORS_ORIGINS: "https://app.rezmerchant.com,https://admin.rezmerchant.com"
  RATE_LIMIT_MAX: "100"
  RATE_LIMIT_WINDOW_MS: "60000"
  MONGODB_DATABASE: "REZ-merchant-trust-bridge"
  REDIS_DATABASE: "0"
