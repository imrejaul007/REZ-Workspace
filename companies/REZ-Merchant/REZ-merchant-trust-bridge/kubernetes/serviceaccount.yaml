# Kubernetes ServiceAccount for REZ-merchant-trust-bridge
apiVersion: v1
kind: ServiceAccount
metadata:
  name: REZ-merchant-trust-bridge-sa
  namespace: rez-merchant
  labels:
    app: REZ-merchant-trust-bridge
    managed-by: rez-merchant
  annotations:
    description: Service account for REZ-merchant-trust-bridge

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: REZ-merchant-trust-bridge-pdb
  namespace: rez-merchant
  labels:
    app: REZ-merchant-trust-bridge
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: REZ-merchant-trust-bridge

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: REZ-merchant-trust-bridge-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: REZ-merchant-trust-bridge
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4041
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
