# Kubernetes ServiceAccount for rez-store-onboarding
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-store-onboarding-sa
  namespace: rez-merchant
  labels:
    app: rez-store-onboarding
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-store-onboarding

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-store-onboarding-pdb
  namespace: rez-merchant
  labels:
    app: rez-store-onboarding
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-store-onboarding

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-store-onboarding-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-store-onboarding
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4032
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
