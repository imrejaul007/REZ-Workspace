# ================================================
# REZ-Merchant Kubernetes ServiceAccount Template
# ================================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-mind-hotel-service-sa
  namespace: rez-merchant
  labels:
    app: rez-mind-hotel-service
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-mind-hotel-service

---

# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-mind-hotel-service-pdb
  namespace: rez-merchant
  labels:
    app: rez-mind-hotel-service
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-mind-hotel-service

---

# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-mind-hotel-service-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-mind-hotel-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4017
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}