# ================================================
# REZ-Merchant Kubernetes Deployment
# ================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: rez-booking-engine
  namespace: rez-merchant
  labels:
    app: rez-booking-engine
    version: latest
    managed-by: rez-merchant
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rez-booking-engine
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: rez-booking-engine
        version: latest
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "4042"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: rez-booking-engine-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: rez-booking-engine
          image: ghcr.io/imrejaul007/rez-booking-engine:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 4042
              protocol: TCP
          envFrom:
            - configMapRef:
                name: rez-booking-engine-config
            - secretRef:
                name: rez-booking-engine-secrets
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp-dir
              mountPath: /tmp
      volumes:
        - name: tmp-dir
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: rez-booking-engine
                topologyKey: kubernetes.io/hostname
