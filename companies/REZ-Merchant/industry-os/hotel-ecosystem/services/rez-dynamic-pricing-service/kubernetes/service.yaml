# ================================================
# REZ-Merchant Kubernetes Service
# ================================================

apiVersion: v1
kind: Service
metadata:
  name: rez-dynamic-pricing-service
  namespace: rez-merchant
  labels:
    app: rez-dynamic-pricing-service
    managed-by: rez-merchant
  annotations:
    description: "rez-dynamic-pricing-service service"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4040
      targetPort: http
      protocol: TCP
  selector:
    app: rez-dynamic-pricing-service

---

# Headless service for StatefulSets (if needed)
apiVersion: v1
kind: Service
metadata:
  name: rez-dynamic-pricing-service-internal
  namespace: rez-merchant
  labels:
    app: rez-dynamic-pricing-service
    managed-by: rez-merchant
  annotations:
    description: "rez-dynamic-pricing-service internal headless service"
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 4040
      targetPort: http
      protocol: TCP
  selector:
    app: rez-dynamic-pricing-service
