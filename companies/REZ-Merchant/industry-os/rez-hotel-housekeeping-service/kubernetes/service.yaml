# ================================================
# REZ-Merchant Kubernetes Service Template
# ================================================

apiVersion: v1
kind: Service
metadata:
  name: rez-hotel-housekeeping-service
  namespace: rez-merchant
  labels:
    app: rez-hotel-housekeeping-service
    managed-by: rez-merchant
  annotations:
    description: "Hotel Housekeeping Service"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4021
      targetPort: http
      protocol: TCP
  selector:
    app: rez-hotel-housekeeping-service