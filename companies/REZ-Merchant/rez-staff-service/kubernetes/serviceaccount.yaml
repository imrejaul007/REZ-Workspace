# Kubernetes ServiceAccount for rez-staff-service
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-staff-service-sa
  namespace: rez-merchant
  labels:
    app: rez-staff-service
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-staff-service

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-staff-service-pdb
  namespace: rez-merchant
  labels:
    app: rez-staff-service
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-staff-service

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-staff-service-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-staff-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4091
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
