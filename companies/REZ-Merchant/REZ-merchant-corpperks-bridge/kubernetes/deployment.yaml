# ================================================
# REZ-Merchant Kubernetes Deployment Template
# ================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: REZ-merchant-corpperks-bridge
  namespace: rez-merchant
  labels:
    app: REZ-merchant-corpperks-bridge
    version: v1.0.0
    managed-by: rez-merchant
spec:
  replicas: ${REPLICAS:-2}
  selector:
    matchLabels:
      app: REZ-merchant-corpperks-bridge
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: REZ-merchant-corpperks-bridge
        version: v1.0.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3005"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: REZ-merchant-corpperks-bridge-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: REZ-merchant-corpperks-bridge
          image: ghcr.io/imrejaul007/REZ-merchant-corpperks-bridge:v1.0.0
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3005
              protocol: TCP
          envFrom:
            - configMapRef:
                name: REZ-merchant-corpperks-bridge-config
            - secretRef:
                name: REZ-merchant-corpperks-bridge-secrets
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp-dir
              mountPath: /tmp
      volumes:
        - name: tmp-dir
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: REZ-merchant-corpperks-bridge
                topologyKey: kubernetes.io/hostname