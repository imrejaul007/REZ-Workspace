# ================================================
# REZ-Merchant Kubernetes Service Template
# ================================================

apiVersion: v1
kind: Service
metadata:
  name: 0
  namespace: rez-merchant
  labels:
    app: 0
    managed-by: rez-merchant
  annotations:
    description: "0 service"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4059
      targetPort: http
      protocol: TCP
  selector:
    app: 0

---

# Headless service for StatefulSets (if needed)
apiVersion: v1
kind: Service
metadata:
  name: 0-internal
  namespace: rez-merchant
  labels:
    app: 0
    managed-by: rez-merchant
  annotations:
    description: "0 internal headless service"
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 4059
      targetPort: http
      protocol: TCP
  selector:
    app: 0