# Kubernetes ServiceAccount for rez-self-checkout
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-self-checkout-sa
  namespace: rez-merchant
  labels:
    app: rez-self-checkout
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-self-checkout

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-self-checkout-pdb
  namespace: rez-merchant
  labels:
    app: rez-self-checkout
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-self-checkout

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-self-checkout-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-self-checkout
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4092
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
