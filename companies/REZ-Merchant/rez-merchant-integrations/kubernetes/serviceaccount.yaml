# Kubernetes ServiceAccount for rez-merchant-integrations
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rez-merchant-integrations-sa
  namespace: rez-merchant
  labels:
    app: rez-merchant-integrations
    managed-by: rez-merchant
  annotations:
    description: Service account for rez-merchant-integrations

---
# PodDisruptionBudget for high availability
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rez-merchant-integrations-pdb
  namespace: rez-merchant
  labels:
    app: rez-merchant-integrations
    managed-by: rez-merchant
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: rez-merchant-integrations

---
# NetworkPolicy for service isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rez-merchant-integrations-network-policy
  namespace: rez-merchant
spec:
  podSelector:
    matchLabels:
      app: rez-merchant-integrations
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 4040
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: mongodb
      ports:
        - protocol: TCP
          port: 27017
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
    - to:
        - namespaceSelector: {}
