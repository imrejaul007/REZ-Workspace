apiVersion: v1
kind: Service
metadata:
  name: waitron
  namespace: hojai
  labels:
    app: waitron
    company: hojai-ai
    industry: restaurant
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4820
      protocol: TCP
      name: http
  selector:
    app: waitron
