apiVersion: v1
kind: Service
metadata:
  name: shopflow
  namespace: hojai
  labels:
    app: shopflow
    company: hojai-ai
    industry: retail
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4830
      protocol: TCP
      name: http
  selector:
    app: shopflow