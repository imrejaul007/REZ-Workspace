apiVersion: v1
kind: Service
metadata:
  name: propflow
  namespace: hojai
  labels:
    app: propflow
    company: hojai-ai
    industry: real-estate
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4807
      protocol: TCP
      name: http
  selector:
    app: propflow