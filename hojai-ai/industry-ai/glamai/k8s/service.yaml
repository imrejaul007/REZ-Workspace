apiVersion: v1
kind: Service
metadata:
  name: glamai
  namespace: hojai
  labels:
    app: glamai
    company: hojai-ai
    industry: salon
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4807
      protocol: TCP
      name: http
  selector:
    app: glamai